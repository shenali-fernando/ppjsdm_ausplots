The aic_mods and auc_mods have two sets of csvs for the different model specifications. 

t15 models = the threshold of removing a group was raised from 10 to 15 individuals in a group, this impacts models fg_size, species and species_size. 

ALSO: 
the t15 models were uses R version 4.6.0 while the t10 models (no t in the name) uses R version 4.5.1 